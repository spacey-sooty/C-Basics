#include <stdio.h>
#include <stdlib.h>

typedef struct {
  int data;
  void *next;
} Node;

Node *head = NULL;

// add a node to the list
Node *addNode(int data)

// remove a node from the list

// insert data to a node

// print the list

void printList();

void printMenu();
